> ⚠ **防混淆声明（副本）**：本件为「审查方·列克星敦」出具的代码审查报告**副本**，送存于项目目录仅作知会与整改跟进之用。**非项目侧自查报告**，最终结论以 `代码审查\` 正本为准。审查方不修改本项目代码；下列「行动项」由项目实施侧自行斟酌执行。

# 代码审查报告 · FFXIV_汉化插件的插件（项目二）— 副本

- **署名**：列克星敦「审查方」
- **审查日期**：2026-09-25 18:30
- **范围**：截至今日的全部未提交改动（14 受跟踪文件 + 2 新测试文件，+920/−131）。

## 结论先行
质量高、测试充分、红线干净，**无阻断性发现**。`dotnet test` **58/58 通过**；最重「后台线程调 ImGui 关窗崩溃」已通过封送渲染线程 + 只枚举字段根治，有 3 条回归测试。仅 1 处低严重静默 catch。改动仍本地未提交（`[ahead 7]`），符合 P0-1（未推），建议本地提交固化。

## 红线扫描
无 `Process.Start`/`cmd`/硬编码路径/凭证/外联地址；仅既有 `ZhipuApiKey` 兼容字段定义 + 关窗处既有 `catch{/*尽力关*/}` 风格。

## 核心改动核实（摘要）
1. **关窗崩溃根治**：`ClosePluginWindowOnFrameworkThread`（封送渲染线程）+ `EnumerateMembers` 只枚举字段（不再调属性 getter，崩因）+ `GetPluginInstance` 两层下钻；3 条新回归测试。
2. **体检重构**：默认「仅清单模式不开窗」绕开崩溃；`/ptp check <名>`/`close`/`health`/`help`；清单按字母序、排除汉化工具系列。
3. **Penumbra 抑制**：单 bool→栈+深度，修 Konekomods「小猫猫」漏翻；`igBegin/igEnd` 常驻；补挂 `igTreeNodeBehavior` 修 Heliosphere 四折叠头全英文；`LabelWithEnd` 区间式替换修中文空白。
4. **联动只读黑名单**：`OldDictionaryService.SetLinkedBlacklistFile` 并集并入；只读旧项目、探测失败静默跳过；只联动单词黑名单、不联动 wiki 黑名单。
5. **串拼接预合并**：`MergeConcatenatedLiterals` + 上限 300→600；8 例回归。

## 行动项（整改建议，可自行发送）
- **F1（低）**：`SourceExtractService.cs` 提取循环 `catch { continue; }` 静默吞异常 → 补一条 Debug/Warn 记录被跳过文件。
- **F2（建议）**：14 文件 + 2 测试仍本地未提交 → 建议 `git commit` 固化；推送须司令官明说（P0-1）。
- **F3（无）**：9 份历轮报告 `.md` 副本非代码，可择期归档。

*副本终。正本见 `代码审查\代码审查报告_FFXIV_汉化插件的插件_20260925_183023.md`。*
