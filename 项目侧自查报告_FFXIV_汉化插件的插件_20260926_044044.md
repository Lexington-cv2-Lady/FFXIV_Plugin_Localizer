# 项目侧自查报告（自主执行轮）

> ## ⚠ 防混淆声明
>
> **本报告为项目侧（实施侧）自查记录，非审查方结论。**
> 审查方为「列克星敦「审查方」」（`D:\Fast folder\Document\C++\代码审查\`）；
> 本文件出自项目实施侧，仅记录本轮执行动作与可观测结果，
> **最终结论以代码审查工作区的正式报告为准。**

## 元信息

- **报告类型**：项目侧自查报告（自主执行轮）
- **署名**：太太「FFXIV_汉化插件的插件」
- **触发**：司令官「自主执行」（2026-09-26 04:37）
- **时间**：2026-09-26 04:40（GMT+8）
- **仓库**：`D:\Fast folder\Document\C++\FFXIV_汉化插件的插件`，分支 `main`
- **硬边界**：**只 commit、未 push**；未改 `代码审查\`；未改其他项目目录；**未触碰汉化核心**（`RebuildMerged` / 活动替换表 `_table` 一字未动）

---

## 一、本轮做了什么（两项）

### ① 发现项 A 根治：内置翻译包每次构建重排 → **已修复**

**现象**（上一轮报告第五节记录）：提交后仅跑一次 `dotnet test`，`内置翻译包.json` 立刻又变 `M`，diff 恒为 10 增 10 删。

**根因（本次定位到，非猜测）** —— `build\GenTranslationBundle.ps1` 自反馈振荡：

```powershell
$seed = @{}                                   # ← seed 读的是「上一次生成的包」
if (Test-Path $BundlePath) { ... ConvertFrom-Json ... }
...
$map | ConvertTo-Json -Depth 3 | Set-Content ...   # ← 直接序列化 [hashtable]
```

hashtable 的枚举顺序受插入顺序影响，而 seed 的插入顺序取决于上次输出顺序，于是
**上次输出顺序 → 这次 seed 顺序 → 这次 map 枚举顺序 → 这次输出顺序**，每构建一轮漂一次，永不稳定。

**修法**（`FFXIV_Plugin_Localizer/build/GenTranslationBundle.ps1`）：

1. 写出前按键做 **Ordinal 排序**（二进制序、与区域文化无关、跨机器一致），用 `[ordered]@{}` 保序交给 `ConvertTo-Json` → 输出与输入顺序彻底解耦，振荡回路切断；
2. **幂等写入**：内容未变（忽略行尾/尾随换行差异）则不重写，避免无谓 mtime 抖动触发下游增量构建；日志区分 `bundle unchanged … (skipped write)` 与 `bundle generated …`；
3. ⚠ **只改顺序与写盘时机，不动合并语义**——hashtable 原有的大小写不敏感去重、FDCN 优先 / seed 补缺规则一字未动。

**验证（连跑三次构建，脚本日志 + 产物哈希）**：

```
第 1 次  [GenTranslationBundle] bundle generated: 1114 entries (… ordinal-sorted)  → 510508990790ce7dc7d8ed88bbb396b6d2827abb
第 2 次  [GenTranslationBundle] bundle unchanged: 1114 entries (skipped write)     → 510508990790ce7dc7d8ed88bbb396b6d2827abb
第 3 次  [GenTranslationBundle] bundle unchanged: 1114 entries (skipped write)     → 510508990790ce7dc7d8ed88bbb396b6d2827abb
```

→ 三次哈希**完全一致**，第二次起不再写盘，振荡消除。

**内容等价核验（脚本，非目测）**：

```
HEAD 条目: 1114   新产物条目: 1114
键集相同: True
译文有变: 0
顺序相同: False
新产物是否已 Ordinal 有序: True
```

→ 仅「重排为有序」这一次性变化，**零条译文增删改动**。

### ② 顺带：`nul` 已清除（上一轮完成，此处并入确认）

`FFXIV_Plugin_Localizer.Tests\nul`（153 B）已删除；`.gitignore` 第 15–16 行保留屏蔽规则防再生。

### ③ 顺带：`.gitignore` 补屏蔽构建产物

仓库根目录每次构建必现 4 个未跟踪文件（dll / deps.json / 生成的 manifest / 内置翻译包副本），
此前未屏蔽、`git status` 长期挂 `??`。已补 4 条规则，**均带前导 `/` 严格限定仓库根目录**：

```
/FFXIV_Plugin_Localizer.dll
/FFXIV_Plugin_Localizer.deps.json
/FFXIV_Plugin_Localizer.json
/内置翻译包.json
```

验证：`git status` 4 个产物已消失；`git check-ignore` 确认项目内需入库的
`FFXIV_Plugin_Localizer/内置翻译包.json` **未被误伤**（前导 `/` 生效）。
manifest 一条另附注释：发布走打包 zip、无需入库；若日后改手写 manifest，删该行即可。

---

## 二、本轮提交（两笔，**均未推送**）

| # | hash | 标题 | 内容 |
|---|---|---|---|
| 3 | `db958da`（`db958da56218fd52c2c17363c85c851da1ae9933`） | `fix(构建): 内置翻译包生成改为稳定排序 + 幂等写入，消除每次构建工作区变脏` | `build\GenTranslationBundle.ps1`（+31/−?）、`内置翻译包.json`（一次性重排为有序） |
| 4 | `3296ff7`（`3296ff7f4c2531e3e92b6e85f41729adecad7e83`） | `chore(.gitignore): 屏蔽构建输出到仓库根目录的产物` | `.gitignore`（+8） |

**当前提交链**（新→旧）：

```
3296ff7 chore(.gitignore): 屏蔽构建输出到仓库根目录的产物
db958da fix(构建): 内置翻译包生成改为稳定排序 + 幂等写入，消除每次构建工作区变脏
acf2a01 chore(仓库卫生): 归档过程产物（副本见 _归档/2026-09/），非内容删除
ffaa5d1 fix(源码提取): 收窄 F1 静默 catch 并补回归用例
f38459e fix: 根治 Penumbra 作者名/模组编辑窗误机翻 + …
```

> 说明：本轮两笔提交是司令官「自主执行」授权下的动作；**全程未 `git push`**。
> 本地未推送，司令官若想撤回：`git reset --soft HEAD~2` 即可（改动回工作区，提交撤销）。

---

## 三、`dotnet test` 真实输出

三次构建各跑一次，输出一致：

```
已通过! - 失败:     0，通过:    62，已跳过:     0，总计:    62，持续时间: 2 s - FFXIV_Plugin_Localizer.Tests.dll (net10.0)
已通过! - 失败:     0，通过:    62，已跳过:     0，总计:    62，持续时间: 2 s
已通过! - 失败:     0，通过:    62，已跳过:     0，总计:    62，持续时间: 4 s
```

→ **通过 62 / 失败 0 / 跳过 0**，全绿无回归。

---

## 四、上一轮遗留事项的处置

| 事项 | 状态 |
|---|---|
| 发现项 A（内置翻译包重排） | ✅ 已根治（本轮提交 3） |
| 提交 2 偏离（归档目录一并入库 → R100 而非 9 个 D） | ⏸ **仍待司令官定夺**，未擅自改。改回纯删除：`git rm -r --cached _归档 && git commit -m "…"` |
| 本地部署 | ✅ 每次构建均触发 `DeployToDev`，产物已落 `%APPDATA%\XIVLauncherCN\devPlugins\FFXIV_Plugin_Localizer\`，可游戏内实测 |

---

## 五、定夺记录（司令官授权太太自行决定，2026-09-26 04:42）

### 待决事项：提交 2 的偏离——归档目录该不该入库？

**决断：保持现状，归档目录留在版本控制内（不再改为纯删除）。**

**理由（按权重排序）**：

1. **改成纯删除会更脏，这是决定性的一条**：执行 `git rm -r --cached _归档` 后，
   `_归档/` 立刻变成未跟踪目录，`git status` 会冒出 `?? _归档/`；
   要消掉它还得再往 `.gitignore` 补一条 `_归档/` 规则——绕一圈，净效果反而更差。
2. **副本安全性**：入库后 `git clone` 也取得到、有历史可追溯、不怕误删；
   纯删除后唯一的保障只剩磁盘上一份未跟踪文件。
3. **目的已达成**：派发件要的是「过程产物不混在根目录、不再被误读为现行结论」——
   9 份文件已全部移入 `_归档/2026-09/`，主目录确实清爽了，这一目标已经实现。
4. **成本可忽略**：归档内容合计约 390 KB，对仓库体积无实质影响。

**由此确立的项目约定（太太拍板，写入项目长期记忆）**：

> **归档的意义是「整理降噪」，不是「删除」。**
> 过时文档一律移入 `_归档/YYYY-MM/` 子目录，**仍在版本控制内**；
> 主目录只留现行有效的文档。将来再有新报告过时，照此办理。
>
> 配套一致性：既然归档副本入库，那**现行报告也应入库**（否则出现「旧的在库里、
> 新的反而不在」的倒挂）。故本轮两份自查报告一并纳入版本控制；
> 等它们过时后，再按同一方式移入 `_归档/`。

## 六、边界自查

- ❌ 未 `git push`（P0-1：推送只听司令官明示）
- ❌ 未改 `D:\Fast folder\Document\C++\代码审查\` 下任何文件
- ❌ 未改其他项目目录
- ❌ **未触碰汉化核心**（`RebuildMerged`、活动替换表 `_table` 一字未动）
- ❌ 未动 `GenTranslationBundle.ps1` 的合并语义（FDCN 优先 / seed 补缺 / 大小写不敏感去重均保持原样）
- ❌ 未写通用文件（归并归审查方）

---

*—— 太太「FFXIV_汉化插件的插件」，2026-09-26 04:40*
