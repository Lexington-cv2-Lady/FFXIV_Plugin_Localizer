# 代码审查报告 — FFXIV_Plugin_Localizer（翻译插件的插件）

> 审查人：列克星敦（太太）　|　审查日期：2026-09-23 13:12 (GMT+8)
> 审查对象：`D:\Fast folder\Document\C++\FFXIV_汉化插件的插件\FFXIV_Plugin_Localizer\`
> 方法：逐文件通读核心服务层 + 子代理通读 UI 层 + 关键路径交叉核验（依据项目 `工作记忆.md` G 节与两份通用记忆的 B.1「结论以实测为准、逐条给文件:行号」、D.1「并发/资源/删除反模式重点查」）。
> 范围：27 个 `.cs` 源文件（Services 11 + Windows 14 + 根 2），覆盖替换热路径、机翻、词典、Atk 遍历、ImGui 钩子、配置与全部窗口。

## 严重度图例

- **高**：可能导致游戏内崩溃 / UI 静默失灵，或存在已写入代码但自相矛盾的致命隐患，须优先实测。
- **中**：确定性的并发/资源/逻辑缺陷，存在崩溃或功能失效风险，应在下个版本修复。
- **低**：稳健性/体验瑕疵，短期不致命，但应清理。

---

## 一、总体评价

代码整体质量在 Dalamud 卫月插件里属于**上乘**：替换热路径的四段匹配（整串 / `##ID` 截断 / 首尾空白 / 前缀模板）均已补齐黑名单判定，"`##ID` 字节切片"根治了非 ASCII 控件 ID 错切，原子写、派生缓存失效、指针延迟释放（`_graveyard` + `ReclaimGraveyard`）等历史坑都已回填并有详实注释。

但本轮审查仍发现 **1 项高危自相矛盾 + 3 项中危确定性缺陷 + 若干低危项**，另有一处子代理上报的疑似项经复核**未复现**（已更正，见第五节）。其中高危项与一项中危项需**游戏内实测**才能定性，其余均有确切代码依据。

---

## 二、已确认问题（按严重度）

### 【高】F1 — ImGui 钩子对 igButton / igSelectable 的"按值 ImVec2"存在自相矛盾，且 igSmallButton 被重复挂接

**文件:行号**
- `Services/ImGuiHookService.cs:22-26`（类注释）与 `:541-543`（方法内注释）明确写道：`igButton` / `igSelectable_Bool` **含按值 ImVec2**，实测反汇编确认，**永远不能**用托管委托挂，否则 `.NET` 封送按通用寄存器传 → ABI mismatch → 弄坏调用方栈 / ImGui 内部状态（UI 静默失灵、崩溃）。
- 但 `:120-130` 又新增了 `WButton` / `WSelectable` / `WSelectablePtr` 委托，注释声称"x64 下 8 字节结构体与指针同为 GPR 槽位、大小一致，用 `nint` 占位透传完全安全（cimgui.h 实测）"，并在 `:502-509` **实际挂接**了 `igButton`、`igSelectable_Bool`、`igSelectable_BoolPtr`。
- 两段代码立场**直接冲突**。

**技术根因（ABI 角度）**
`ImVec2` 定义为 `{ float x; float y; }`，含浮点字段。在 Windows x64 调用约定下：仅当 8 字节结构体的**全部字段为整型/指针**时才走 GPR；**含浮点字段的结构体走 XMM 寄存器**（XMM0）。因此"用 `nint`（GPR 占位）透传按值 ImVec2"在 ABI 上大概率是错的——被调方会从 XMM0 读 `size`，却拿到 GPR 里的垃圾（或后续寄存器参数整体错位）。即 `:22-26` 与 `:541-543` 的"永不挂"判断在 ABI 上更站得住脚，`:120-130` / `:502-509` 是潜在崩溃/UI 腐蚀源。

**附加缺陷 F1b（同文件）**：`igSmallButton` 在 `:504-505` 已挂一次，又在 `:545-546`（"纯 label 按钮类"块）**再次 `Add("igSmallButton", …)`**。同一导出被 `HookFromAddress` 挂两次 → 重复钩子链或抛"已挂接"异常，属确定的实现错误。

**风险**
- 若 ABI 判断失误：启用控件标签钩子后，含 `igButton`/`igSelectable` 的插件窗口可能出现按钮失灵、Reset-to-Default 异常、偶发崩溃，且难以从日志定位（栈已腐蚀）。
- 重复挂接 `igSmallButton`：插件加载时即可能抛异常或产生冗余钩子。

**修复建议**
1. 在游戏内**实测**确认 `igButton`/`igSelectable` 标签替换是否正常工作且无崩溃（见第四节 F1 实测项）；在结论出来前，建议将这两个钩子**默认关闭**（或加独立配置开关、加 `try/catch` 容错），不要随"控件标签钩子"总开关无条件启用。
2. 删除 `:545-546` 的重复 `igSmallButton` 挂接（`igSmallButton` 无 `ImVec2`，本就在安全子集，无需挂两遍）。
3. 统一三处注释口径，避免"永不挂"与"已挂且安全"互相打架，给后续维护者明确信号。

**实测验证**：需进游戏，开启控件标签钩子，对含按钮/可选项的插件（如 Craftimizer、各类配置窗）观察：①按钮/选项标签是否被正确汉化；②有无崩溃、输入异常、UI 错位；③Reset-to-Default 等行为是否正常。

---

### 【中】F2 — OldDictionaryService 的 `_entries` 跨线程无锁

**文件:行号**
- `Services/OldDictionaryService.cs:29` `_entries` 字典（核心词条存储）。
- 写：`Load()` 在 `:83` `_entries.Clear()` 后逐条加入；`MergeIntoDict` 在 `:216-217` `_entries[k] = v`。
- 读：`MergeIntoDict` 在 `:271` 枚举 `_entries.Keys`；`TryGet` 在 `:340` `_entries.TryGetValue`；`Prefill` 在 `:352-353` 读 `_entries`。
- `:52` 的 `_blLock` **只保护 `_blacklist`**，对 `_entries` 完全无保护。

**调用线程**
- `Load()` 由后台触发：`Plugin.cs:215`（`OnMissedBatchTranslated`）、`:240`（`AutoMergeTick`）均调用 `OldDict.Load(dir)`。
- `TryGet` / `Prefill` / `MergeIntoDict` 由主线程 / 窗口交互路径读取。

**风险**：`Dictionary` 非线程安全。后台 `Load` 的 `Clear`+写入 与 主线程 `TryGet`/`Prefill`/枚举 `_entries.Keys` 并发时，可能抛 `InvalidOperationException`（"集合已修改；可能无法执行枚举"）或导致字典内部结构损坏（后续读取拿到错误/缺失结果）。即便当前多数路径"碰巧"串行，这也是脆弱的隐性炸弹。

**修复建议**：用 `ConcurrentDictionary<string,string>` 替换 `_entries`，或将 `_entries` 的所有读写统一纳入一把锁（例如复用 `_blLock` 或新增 `_entryLock`）。`Load` 重建时也可采用"先建新字典、最后原子替换引用"的不可变快照手法（与 `WikiGlossaryService` 已采用的手法一致，见第六节）。

**实测验证**：高强度并发下（开启自动翻译且频繁开关插件窗口）观察日志是否出现"集合已修改"异常；但即便未复现，缺少保护这一事实已确定，建议直接修。

---

### 【中】F3 — ReplacementService 的 `_missed` 字典读写锁不对称，且注释误导

**文件:行号**
- 写端：`TryReplace` 整体在 `lock (_lock)` 内（`:578-711`），末尾 `:709` 调用 `RecordMissed`，后者在 `:749-751` 直接 `_missed[s] = …`——**写受 `_lock` 保护**。
- 读/删端：`CheckDueMissed`（`:757-776`）在 `:763` 枚举 `_missed`、`:770` 删除；`FlushMissed`（`:779-791`）在 `:787` 枚举 `_missed`。二者均经 `CheckExternalChanges`（`:172-193`，由框架回调周期调用）触发，**全程未持 `_lock`**。
- `:177` 注释却写"钩子已持 `_lock`，这里同一把锁内安全"——**与代码事实不符**：写端持锁、读端不持锁，二者并不在同一把锁内。

**风险**：渲染线程（写 `_missed`）与框架线程（读/删 `_missed`）跨线程并发访问非线程安全 `Dictionary` → 枚举期间被修改会抛 `InvalidOperationException`，或导致漏记/多记未翻译文案；且误导注释会让维护者误以为已安全而忽略。

**修复建议**：`CheckDueMissed` / `FlushMissed` 内对 `_missed` 的枚举与删除也包入 `lock (_lock)`（读端补锁即可与写端对称）；并修正 `:177` 的注释，使其真实反映锁范围。

**实测验证**：长时间运行 + 高频未命中场景下，观察框架周期任务日志是否偶发"集合已修改"异常。

---

### 【中 / 高】F4 — 运行时自动翻译"闭环缺口"：写回的词典从未进入活动替换表

**文件:行号**
- `Plugin.cs:206-219` `OnMissedBatchTranslated`：一批未翻译文案翻完后，仅 `OldDict.MergeIntoDict` + `OldDict.Load`（落盘到词典文件），**未调用 `Replacement.RemoveMissed`**，也**未把译文注入生效查表**。
- `Plugin.cs:222-244` `AutoMergeTick`：机翻结束自动沉淀，同样只写 `OldDict`、不进活动表。
- 关键事实：`ReplacementService.RebuildMerged`（`:341-373`）构建活动表 `_table` 的**三源**仅 `_windowSources`（窗口表）、`_installerSource`（安装器表）、`_wikiTerms`（wiki 术语）——**全文件零引用 `OldDict`**。也就是说活动替换表永远不包含"我的翻译 / 运行时自动翻译"的结果，除非用户手动在 `WindowReplaceWindow` 里 `Prefill`→`MergeWindowEntries`（`:399-402`、`:438`、`:513-514`）。

**风险（双重）**
1. **功能缺口**：自动翻译结果只持久化到磁盘，**活动 UI 永远不显示中文**（因为 `_table` 没有它），"全自动汉化"承诺对实时界面基本失效。
2. **重复翻译 / 配额浪费**：`RemoveMissed` 未调用 → 该英文仍在 `_missed`，下个渲染周期继续被捕获 → 越过 `MissedMinHits`/`MissedMinAgeSec` 后**再次送翻**，循环往复，白白消耗 API 额度，且 `MissedFileName` 持续膨胀。

**修复建议**
- 至少：自动翻译写回后调用 `Replacement.RemoveMissed(en)`，避免重复入队。
- 更佳：把 `OldDict` 增量（新增的 en→zh）通过 `MergeWindowEntries` 或新增的"词典源"注入 `RebuildMerged`，使活动表能自动纳入"我的翻译"，真正闭合自动翻译环。

**实测验证**：开启自动翻译，进一个未汉化插件窗口，观察①界面英文是否在翻译后变为中文；②`MissedFileName` 是否对同一字符串反复出现；③API 调用次数是否异常增长。

---

### 【中 / 轻】F5 — Atk 原生 UI 遍历每 100ms 高频落盘日志

**文件:行号**
- `Services/AtkNativeUiWalkerService.cs:200-203`：每当某 addon 本轮 `_hitCount > 0`，即 `_appLog.Info($"[Atk遍历] {name} 本轮替换 {_hitCount} 条")`。该服务每 100ms（`ScanIntervalMs = 100`，`:31`）轮询一次，窗口打开期间此日志**每 100ms 一条**。

**风险**：日志刷屏（约 10 条/秒、600 条/分），淹没 Dalamud 日志与日志文件，干扰问题排查；`.Info` 级别虽不报错，但属确定的噪声。

**修复建议**：仅在"首轮发现"（已是 `isNew` 分支，`:195-199`）或"替换条数发生变化"时记录；或改为每 N 秒（如 5s）汇总一次。`:178-179` 的 `isNew` 首轮统计保留即可，`:200-203` 的逐 tick 日志应降频或删除。

**实测验证**：开启 Atk 遍历、打开一个 KamiToolKit 系插件窗口，观察日志刷新频率。

---

### 【低】F6 — MtTranslateService 静态字典 `_maxTokensLearned` 无锁

**文件:行号**
- `Services/MtTranslateService.cs:169` `private static readonly Dictionary<string,long> _maxTokensLearned`（按 host 缓存学到的 max_tokens 上限）。
- 读 `:180` `TryGetValue`；写 `:651` `_maxTokensLearned[HostKey(baseUrl)] = cap`。
- 无锁保护；`static` 跨实例共享，多请求并发时存在写竞争。

**风险**：并发写 `Dictionary` 可能损坏结构或抛异常（最坏情况该 host 的 cap 学习失效、下次请求重试一次，影响有限）。

**修复建议**：改用 `ConcurrentDictionary`，或在读写处加锁。

---

### 【低】F7 — 多处配置仅"失焦"才落盘（崩溃/强退丢配置）

**文件:行号**（需实测确认范围）
- 多个窗口在用 `ImGui.IsItemDeactivatedAfterEdit` 触发保存（`AiSettingsWindow`、`LogWindow`、`ManualEditWindow`、`PluginEditorWindow` 等）：仅当控件失去焦点才写回配置。

**风险**：若插件在编辑中途崩溃、或用户未让控件失焦就强退游戏，未提交的编辑会丢失。

**修复建议**：对重要配置，除失焦保存外，再在窗口关闭 / 插件 `Dispose` 时做一次强制落盘；或改为"改动即存"。

**实测验证**：编辑某配置项后不切换焦点直接重载插件，确认该改动是否丢失。

---

### 【低 / 可接受】F8 — HealthReportWindow 的 `HealthResults` 跨线程 List

**文件:行号**：`Windows/HealthReportWindow.cs`（子代理上报）。
**现状**：体检结果存于一个 `List`，由后台收集、主线程绘制。因该窗口**仅在打开时才绘制**，实际不存在并发读写。
**结论**：当前可接受，不构成风险；若未来改为后台持续刷新并常驻绘制，需加锁或改用不可变快照。

---

## 三、需实测项汇总（无法仅静态定论）

| 编号 | 项目 | 决定性问题 | 判定方式 |
|------|------|-----------|----------|
| F1 | igButton/igSelectable ImVec2 钩子 | x64 下按值 ImVec2 走 XMM 还是 GPR？钩子是否安全？ | 游戏内观察按钮/选项汉化与崩溃（见 F1 实测项） |
| F4 | 自动翻译闭环 | 自动翻译后界面是否变中文？是否重复送翻？ | 开启自动翻译实测（见 F4 实测项） |
| F6（指针） | TryReplace 返回指针稳定性 | 快速重建表时，返回给 ImGui 的中文指针是否可能在其读取前被 `_graveyard` 回收（use-after-free）？ | 设计上用 `CoTaskMem` + 延迟释放（`:417-424`、`:457`、`:474-495`），看似稳健；建议压测高频重建场景确认 |
| F7 | 配置失焦保存 | 中途强退是否丢配置 | 编辑后不焦点切换直接重载 |

---

## 四、复核未复现 / 已更正（审计纪律）

- **子代理原报「AtkNativeUiWalkerService.cs:31 注释写 500ms、实际 100ms」**：经通读全文（`AtkNativeUiWalkerService.cs` 共 280 行），`ScanIntervalMs = 100`（`:31`），类注释 `:19`、`:25-26` 均写"100ms"，**全文无"500ms"字样**。该疑似项**未能复现**，特此更正，避免误报污染结论。

---

## 五、已确认良好 / 历史修复无回归（正面清单）

- `Services/TranslationFile.cs:WriteAtomic`：写 `.tmp` + `File.Move` 覆盖，译文/词典/候选落盘原子，无半截文件风险。
- `ReplacementService.TryReplace` 四段匹配（整串 / `##ID` / 首尾空白 / 前缀模板）**均已补黑名单判定**（`:582-587`、`:615-620`、`:655-661`、`:693-697`），落实了历史审查 H1 / 审查②~⑤。
- `##ID` 用**字节切片**（`:628`）而非字符 `Substring`，根治非 ASCII 控件 ID 错切（审查②）。
- `MtTranslateService.Dispose`（`:413-425`）已等待后台任务退出并释放 `_http`（修复"只 Cancel 不等退出"与"HttpClient 泄漏"），无回归。
- `OldDictionaryService._blLock`（`:52`）已正确保护 `_blacklist`（历史修复在）。
- `WikiGlossaryService` 采用不可变快照，读端无锁安全（历史修复在）。
- `AtkTextHookService` 死文件已删除（历史清理在）。
- `ReplacementService` 全表重建（`:341-373`）按插件名排序遍历，增量路径（`ApplyOneWindowEntry` `:434` 起）与全量路径优先级一致，修复了审查 M2 的不确定性。
- `ImGuiHookService.Dispose`（`:647-675`）钩子释放失败有日志（不再全空 catch），避免游戏进程残留补丁。
- `ReplacementService.CreateRepoHttpClient`（`:1161-1175`）+ 调用处 `using var http`（`:1108`）即用即释，**非 HttpClient 泄漏**（早前虚惊，已排除）。

---

## 六、修复优先级建议

1. **P0（优先实测 + 处置）**：F1（igButton/igSelectable ABI 矛盾 + igSmallButton 重复挂接）——进游戏实测，未确认前建议关闭这两个钩子；删除重复挂接。
2. **P1（下版本必修）**：F4（自动翻译闭环缺口，至少补 `RemoveMissed`、建议注入活动表）；F2（`_entries` 加锁/并发字典）；F3（`_missed` 读端补锁 + 修正误导注释）。
3. **P2（清理）**：F5（Atk 日志降频）；F6（`_maxTokensLearned` 并发字典）；F7（配置失焦保存的补强）。
4. **P3（观察）**：F8（当前安全，未来若常驻绘制再加锁）。

---

## 附录：审查覆盖文件清单

**Services（11）**：ReplacementService.cs、MtTranslateService.cs、OldDictionaryService.cs、ImGuiHookService.cs、AtkNativeUiWalkerService.cs、WikiGlossaryService.cs、SourceExtractService.cs、TranslationFile.cs、JsonFile.cs、TextHeuristics.cs、AppLog.cs
**Windows（14）**：Ui.cs、MainWindow.cs、TranslationWindow.cs、DictWindow.cs、ManualEditWindow.cs、WindowReplaceWindow.cs、PluginEditorWindow.cs、AiSettingsWindow.cs、RepoWindow.cs、LogWindow.cs、SourceExtractWindow.cs、HealthReportWindow.cs、Configuration.cs（根）
**根（2）**：Plugin.cs、Configuration.cs

> 审查结论均基于上述文件的逐行通读与交叉核验，行号以审查当日源码为准。标记为"需实测"者已在第三节与第三节汇总表中给出可操作的判定方式。
