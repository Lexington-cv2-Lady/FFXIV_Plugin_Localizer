# 项目侧自查报告 · FFXIV_汉化插件的插件

> **⚠ 防混淆声明：本报告为项目侧自查，非审查方结论 —— 以 `D:\Fast folder\Document\C++\代码审查\` 工作区出具的正式报告为准。**

## 元信息

- **报告类型**：项目侧自查（整改件执行回报）
- **目标项目**：`FFXIV_汉化插件的插件`（`/plocalizer`，卫月 Dalamud 插件）
- **署名**：太太「FFXIV_汉化插件的插件」
- **派发方**：列克星敦「审查方」（2026-09-26 整改件）
- **生成时间**：2026-09-26 03:21（本机）
- **执行范围**：① 采纳「写入者标注」新规并复核 09-25 归并条目；② 处置遗留发现项 F1（静默 catch）
- **边界遵守**：未修改 `代码审查\` 下任何文件；未改动其他项目目录；**未执行 git push**（P0-1）；本地改动**未提交 git**

---

## 结论先行

| 项 | 结果 |
|---|---|
| 一、写入者标注新规 | ✅ 已采纳；通用型 6 条复核完毕（4 条本就合规，2 条补齐「来源项目」）；**另主动补齐清单外 2 条**（C.10 / C.10-1，有日日志铁证可归属） |
| 一、FFXIV 侧 7 条（B.6/D.6/D.7/E.6/I.5/I.6/I.7） | ⚠ **非妾身 09-25 归并产物** —— 经查全文件无任何「太太」署名、亦无「来源项目」标注，7 条均带 `〔写入：列克星敦「审查方」·2026-09-26〕`。妾身**无法确证其来源项目**，按新规第 3 条「宁缺勿假」**未擅自补标**，请审查方自行补 |
| 二、F1 静默 catch | ✅ 已处置，选 **(a) 补日志与注释**；新增可测 seam + 4 条回归用例；`dotnet build` 0 警告 0 错误，`dotnet test` **62/62 全过** |

---

## 一、「写入者标注」新规（2026-09-26 立）

### 1.1 今后执行口径（已立为本项目长期约定）

- 凡向 `通用型工作记忆.md` / `FFXIV_汉化通用工作记忆.md` 归并条目：
  1. 条目**末尾**附 `〔写入：太太「FFXIV_汉化插件的插件」·YYYY-MM-DD〕`；
  2. **证据句**须标 `（来源项目 `FFXIV_汉化插件的插件`）`；
  3. 二者是两个维度：来源项目写证据段、写入者写末尾，审查方代归并时分别标。
- 只约束**新增条目**；无法确证归属的旧条目一律标 `〔写入：归属待定〕`，**不臆造归属**。

### 1.2 复核结果（逐条）

| 文件 | 条目 | 复核前 | 处置 | 复核后 |
|---|---|---|---|---|
| 通用型 | **B.33** 验证脚本须与产品代码同源 | 末尾有署名；证据含「来源项目 `FFXIV_汉化插件的插件`」 | 无需改 | ✅ 合规 |
| 通用型 | **B.34** 原生 UI API 只能框架线程调用 | 有署名；证据写「**本项目**"体检关窗"…」——**来源项目缺失且指代模糊** | **已补**：改为「**证据**（来源项目 `FFXIV_汉化插件的插件`）：**其**"体检关窗"…」 | ✅ 已补齐（第 103 行） |
| 通用型 | **B.35** 遍历非泛型集合要写元素类型 | 署名 + 来源项目齐全 | 无需改 | ✅ 合规 |
| 通用型 | **C.12** MSBuild 内联 powershell 多行被 cmd 拆散 | 有署名；证据写「**本项目** `GenTranslationBundle`…」——**来源项目缺失** | **已补**：改为「**证据**（来源项目 `FFXIV_汉化插件的插件`）：**其 csproj 的** `GenTranslationBundle`…」 | ✅ 已补齐（第 134 行） |
| 通用型 | **C.13** 子进程 UTF-8 输出注入 `LC_ALL` | 署名 + 来源项目齐全 | 无需改 | ✅ 合规 |
| 通用型 | **C.14** dnfile 解析 .NET 程序集 API 坑 | 署名 + 来源项目齐全 | 无需改 | ✅ 合规 |

### 1.3 清单外主动补齐（有铁证可归属，非臆造）

- **通用型 §C.10**（Git Bash 禁用 CMD 风格 `2>nul`）：末尾补 `（来源项目 `FFXIV_汉化插件的插件`）〔写入：太太「FFXIV_汉化插件的插件」·2026-09-25〕`。
  - **归属铁证**：本项目日日志 `.workbuddy\memory\2026-09-25.md:112` 明确写「（通用版见 `通用型工作记忆.md` §C.10）」，且事故本体（误建 `FFXIV_Plugin_Localizer.Tests/nul`）就发生在本项目。
- **通用型 §C.10-1**（源码提取须先合并字符串拼接链）：同样补来源项目 + 署名。
  - **归属铁证**：日日志 `2026-09-25.md:79` 明确写「字符串拼接链合并的通用教训：见 `通用型工作记忆.md` §C.10-1」，且内容（Heliosphere「技术信息」577 字符、300→600 上限）即本项目 09-25 的修复。

> 现通用型文件中带妾身署名的条目共 **8 条**（B.33/34/35、C.10/10-1/12/13/14），**全部**同条含「来源项目」标注 —— 逐条 grep 复核通过。

### 1.4 FFXIV 侧 7 条：无法归属，**未补标**（请审查方自行补）

`FFXIV_汉化通用工作记忆.md` 中 **B.6 / D.6 / D.7 / E.6 / I.5 / I.6 / I.7** 七条：

- 经全文 grep，该文件**不存在**任何 `太太「FFXIV_汉化插件的插件」` 署名、也**不存在**任何「来源项目」标注；
- 七条均带 `〔写入：列克星敦「审查方」·2026-09-26〕`，即**审查方 09-26 写入**（非妾身 09-25 归并）；
- 其证据段未写来源项目，但**来源归属只有审查方知道**（如 I.5/I.6/I.7 明显是两个 C# 卫月工程的共性经验，E.6 涉及 Penumbra 模组目录，难以判定出自哪个项目）。
- **处置**：按新规第 2 条「只标注你确可归属的条目、无法确证的一律不臆造」，**不改、不补**，在此如实回报。**建议审查方自行补齐这 7 条的「来源项目」标注。**

---

## 二、遗留发现项 F1：静默 catch（已处置）

### 2.1 问题定位（复核属实）

- **位置**：`FFXIV_Plugin_Localizer\Services\SourceExtractService.cs` 原 **第 714 行**：

  ```csharp
  string text;
  try { text = File.ReadAllText(file, Encoding.UTF8); } catch { continue; }
  ```

- **危害**：`catch { }` 无类型过滤、无日志 —— ① 文件被独占/无权限/枚举后被删时**静默跳过**，用户只看到"少了几条"而无从知晓；② 更严重的是**连真正的代码 bug（空引用、编码错误、OOM）也一并吞掉**，排查时毫无线索。

### 2.2 处置：选 **(a) 补日志与注释**（理由见下）

> **为何不选 (b) 去掉捕获、向上传播**：此处扫的是**第三方插件源码目录**（含只读文件、被编辑器/杀软/并行构建独占的临时文件、枚举与读取之间存在时间差因而可能已被删除的文件）。「个别文件读不到」是**常态而非故障**；一旦上抛，一个坏文件就会毁掉整仓库提取结果（明明有 300 条文案却报"提取失败"）—— 代价远大于收益。故保留捕获，但**写明理由 + 收窄范围 + 强制留痕**。

**改动清单**（`SourceExtractService.cs`）：

| 行号（改动后） | 内容 |
|---|---|
| 706 | 新增 `var skipped = 0; // 读不到的文件数（被占用 / 无权限 / 枚举后被删）` |
| **714–721** | 原静默 catch 替换为：`if (!TryReadSourceText(file, out var text)) { skipped++; continue; }`，上方补 3 行注释说明「属预期、跳过并计数、末尾汇总 Warn、**不静默吞**」 |
| **769–772** | 循环结束后：`if (skipped > 0) _appLog.Warn($"[源码] 有 {skipped} 个 .cs 文件读不到（被其它进程占用 / 无读取权限 / 枚举后被删除），已跳过；其余文件照常提取。")` |
| **776–804** | 新增 `internal static bool TryReadSourceText(string path, out string text)`：方法体注释**逐条写明「此处异常属预期」的三点理由**；捕获范围收窄为 `IOException`（含 FileNotFound / DirectoryNotFound / 共享冲突）`/ UnauthorizedAccessException / SecurityException`，**其余异常照旧上抛**；失败路径返回确定的 `string.Empty`（不留 null 隐患） |

### 2.3 回归用例（B.26 红线：改高危逻辑须配回归）

新增文件 **`FFXIV_Plugin_Localizer.Tests\SourceExtractReadGuardTests.cs`**（4 条，xUnit）：

| 用例 | 守住的行为 |
|---|---|
| `Read_NormalFile_ReturnsTrueWithContent` | 正常文件读得到，UTF-8 中文正确解出（不因改动误伤主路径） |
| `Read_LockedByOtherProcess_ReturnsFalseWithoutThrowing` | 被 `FileShare.None` 独占 → 返回 false、**不抛**（Windows 实测 IOException 路径） |
| `Read_MissingFile_ReturnsFalseWithoutThrowing` | 文件不存在（枚举后被删的典型形态）→ 返回 false、不抛 |
| `Read_Failure_DoesNotReturnTrue` | 失败绝不伪装成成功 —— 保证调用方能据此计数并打 Warn（**不静默吞**的可执行断言） |

> 测试工程经主工程 `<InternalsVisibleTo Include="FFXIV_Plugin_Localizer.Tests" />`（csproj:30）访问 internal seam，与既有 `SourceExtractConcatTests` 同款写法。

### 2.4 实测输出（`dotnet build` / `dotnet test` 真实结果）

**构建**（`DALAMUD_HOME=C:/Users/Match/AppData/Roaming/XIVLauncherCN/addon/Hooks/dev`，本机已有 Dalamud 开发目录，无需降级方案）：

```
FFXIV_Plugin_Localizer -> ...\bin\Debug\FFXIV_Plugin_Localizer.dll
已成功生成。
    0 个警告
    0 个错误
已用时间 00:00:03.67
```

**测试**（`dotnet test FFXIV_Plugin_Localizer.Tests.csproj --configuration Debug -p:DevPluginDir="C:/Temp/wb_test_devplugins"`）：

```
已通过! - 失败:     0，通过:    62，已跳过:     0，总计:    62，持续时间: 496 ms
                    - FFXIV_Plugin_Localizer.Tests.dll (net10.0)
```

- **通过 62 / 失败 0**（改动前基线 58 → 新增 4 条，无回归）。
- **构建即部署已核验**：`devPlugins\FFXIV_Plugin_Localizer\FFXIV_Plugin_Localizer.dll` 与 `bin\Debug\` 产物**同为 364544 B、时间戳同为 09-26 03:19**（`DeployToDev` 自动完成），司令官可在游戏内直接重载验证。

---

## 三、附带发现（未处置，供审查方/司令官定夺）

1. `SourceExtractService.cs:699–700` 有**重复的两行 `<summary>` 文档注释**（第二行是更完整的版本）—— 纯冗余、不影响编译（本项目未开 XML 文档生成故无 CS1587 警告）。**本次未动**（超出 F1 整改范围），若司令官愿妾身顺手清掉，一句话即可。
2. `FFXIV_Plugin_Localizer.Tests\nul`（153 字节，Windows 保留设备名）实体残留仍在 —— 已由 `.gitignore` 屏蔽，不影响构建/测试/发布；需彻底清除只能在 CMD 下 `del "\\?\<绝对路径>\nul"`。

---

## 四、边界与未决

- ✅ 未修改 `D:\Fast folder\Document\C++\代码审查\` 下任何文件；未改动其他项目目录。
- ✅ **未执行 git push**（P0-1：推送只听司令官明示）；本轮改动**尚未本地提交**，待司令官定夺。
- 本轮改动文件合计 3 个：`SourceExtractService.cs`（改）、`SourceExtractReadGuardTests.cs`（新增）、`通用型工作记忆.md`（补标注）。

— 太太「FFXIV_汉化插件的插件」，2026-09-26
