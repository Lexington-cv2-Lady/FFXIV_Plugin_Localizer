# 参考方案 v2：F4 自动翻译闭环缺口（含项目侧商榷的裁决）
>
> **【副本说明 · 防混淆】** 本文件由**代码审查工作区**出具，供 `FFXIV_汉化插件的插件` 项目侧实施参考（**v2，取代 v1**）。
> - **正本**：`D:\Fast folder\Document\C++\代码审查\F4自动翻译闭环_参考方案_FFXIV_汉化插件的插件_v2_20260924_021252.md`
> - **性质**：**参考建议**，**不是**「代码审查报告」——**不触发**第6条「_已修正」处置，也**不属**第7条待核对象。
> - **出具方边界**：本工作区只出建议、**不修改项目代码**；是否实施由司令官定夺、实施由项目侧执行。
> - **版本**：v1（`20260924_000441`）经项目侧附「商榷」后，本 v2 已就三条商榷**逐条裁决**；v1 与含商榷副本保留备查。
> - **防漏读**：配套审查报告 `代码审查报告_记忆体系专项_20260923_233843.md`（本工作区）。
>

- **对象**：`FFXIV_汉化插件的插件`（FFXIV_Plugin_Localizer）
- **提出方**：代码审查工作区（**仅提供建议，不参与代码修改**）
- **署名**：列克星敦「审查方」
- **日期**：2026-09-24（v2；v1 为 `20260924_000441`，项目侧已附「商榷」）
- **依据**：131242 报告 F4 条 + 本工作区 Grep 实测 + 项目侧「附：项目侧商榷」三条
- **配套文件**：本工作区 `代码审查报告_记忆体系专项_20260923_233843.md`；v1 `F4自动翻译闭环_参考方案_FFXIV_汉化插件的插件_v1_20260924_000441.md`
- **一句话结论**：自动翻译只把译文落盘进词典、**不进生效查表**，且未清未命中队列 → 界面永远不变中文，同时同一原文反复送翻、白烧配额。**v2 已就项目侧三条商榷逐条裁决**，可据此实施。
- **★ 状态：已执行（2026-09-24 03:19 提交 `fa7c702`，项目侧实施）——本方案已非待办，保留供追溯与回归对照。** 落地内容：① `ReplacementService` 新增第四源 `_dictTerms`（置 wiki 之后作最低兜底）；② `RemoveMissed` 补 `lock(_lock)`；③ `Configuration.AutoClosureEnabled` **默认关**。实测在位：`Plugin.cs:216/224/254/458/543`、`ReplacementService.cs:870` 起。
- **行动项（待项目侧执行；本工作区不改代码）**：①给 `RemoveMissed`（`ReplacementService.cs:812`，**实测无锁**）补 `lock(_lock)`；②新增**独立第四源**＝运行时词典 / OldDict，置于**最低兜底**（窗口 > 安装器 > wiki > OldDict）；③两步**配套上线**，新增开关**默认关**。

---

## 一、v1 与项目侧商榷的结论

项目侧核实后确认：v1「问题现状」的三条断言**全部与代码一致**（`RebuildMerged` 三源确无 OldDict；`RemoveMissed` 确无锁；调用点仅 `DictWindow.cs:159/167`，`Plugin.cs` 两个回调只做 `MergeIntoDict` + `Load`）→ **F4 成立，值得修**。

项目侧提了三条商榷，v2 逐条裁决如下。

---

## 二、裁决一：新增「独立第四源」，不并入窗口表（对应商榷一）

- **不采纳乙案（写窗口表）**：窗口表按插件组织（`<插件名>.json`），运行时发现的文本未必有明确归属插件，写入等于**伪造归属、污染数据语义**，比加一层源更糟。
- **软化甲案**：不动窗口表语义（窗口表＝插件译文，保持不变），**新增独立第四源**＝运行时词典 / OldDict；同时把项目既定「两套独立」原则**显式升级为四层并写死优先级**（窗口 > 安装器 > wiki > OldDict），让耦合**显性化、可追溯**，而非隐性混用。
- 即：原则由「两套独立」扩为「四层分明」，属**受控的原则演进**（**此处仅指方案深度**按司令官「一次做到底」拟定；**不含任何改代码的授权**——本工作区只出建议、不参与代码修改）。
- ⚠ **实施授权另计（务必注意）**：本方案触及 `RebuildMerged` 与活动替换表，属**汉化核心**。是否动工须由司令官对**项目侧**明示；**未获明示前请勿改动核心逻辑**。若获准，仍须：先备份 / 提交快照、新增开关**默认关**、按第六 / 七节做验证与回滚。

## 三、裁决二：OldDict 置于 wiki **之下**作兜底（对应商榷二 · 采纳项目侧意见）

最终优先级：**窗口表 > 安装器表 > wiki 官方译名 > OldDict（我的翻译 / 运行时译文）**。

理由：
1. **通用 B.22「判据宁可漏判别误杀」**：过译＝用户看得见的错误；漏译＝只是少翻一些，危害小得多。故让官方译名优先，运行时译文只在都没命中时**补缺**。
2. `我的翻译.json` 约 1932 条、含大量副本 / 地名等专名与机翻沉淀，质量参差，**不适合压过** 6.2 万条 wiki 官方译名。
3. 补缺定位恰是运行时翻译的价值所在——wiki 没覆盖的才走 OldDict。

附（顺带统一记忆）：项目记忆 `:475`（wiki 降为兜底）与 `:506`（wiki 最高优先级）**两处不一** → 建议以**代码为准**统一为「窗口 > 安装器 > wiki」，再把新源插在 wiki 之后；记忆 `:706`「全自动 missed 机翻闭环」措辞偏满，宜补注为「**写词典闭环**（词典非替换源）」，以免日后误判为已闭合。

## 四、裁决三：两步配套上线，开关默认关（对应商榷三 · 采纳）

- **第 1 步不单独上线**：只做 `RemoveMissed` 会出现「不再重复送翻、但界面依旧英文」，问题被藏起来，且该原文**永久失去补救机会**。
- **两步配套实施**；新增「自动翻译闭环」开关**默认关**，与项目侧记忆 `:596`「后台自动翻译开关（09-18 用户要求：全部默认关）」一致。

---

## 五、实施清单（仅为步骤建议，不含代码改动）

1. `ReplacementService.RemoveMissed`（:812）内部补 `lock(_lock)`，与 F3 修复**同一把锁**。
2. `RebuildMerged` 增加第四源 OldDict，优先级按第三节；若 `_table` 有派生缓存（哈希桶 / 原生指针），**注入后一并失效**。
3. 增量路径与全量重建**优先级必须一致**（通用 B.19）；注入不得覆盖已有手动词条（FFXIV 母本 B.4）。
4. `OnMissedBatchTranslated` / `AutoMergeTick` 调用 `RemoveMissed` 止血——**与第 2 步同步上线**。
5. 新增开关，默认关。

## 六、不变量（改时不得破坏）

1. 优先级：窗口 > 安装器 > wiki > OldDict，增量与全量一致。
2. 派生缓存随源失效。
3. `_missed` 全部读写（**含 `RemoveMissed`**）在同一把 `lock(_lock)` 内。
4. 热路径零分配：合并 / 发布在后台做，前台读不可变快照（通用 B.24）。
5. 只增不覆盖已有手动词条；黑名单命中保留英文不译。

## 七、验证与回滚

- **验证（按优先级）**：①真实环境开自动翻译，进未汉化插件窗口 → 看界面是否变中文、`MissedFileName` 是否还反复出现、API 调用是否异常；②沙盒副本跑脚本（改数据逻辑必做，别拿真实译文试）；③压测确认 `RebuildMerged` / `Reload` 不回到 100ms 以上（`dalamud.log` 无 Long UiBuilder 告警）。
- **回滚**：先备份 / 提交快照（参照 `48b51fd` 范式）；异常时先关开关（默认关，风险可控），必要时回滚注入。

---

> v2 为**参考建议**，不含任何代码改动；是否实施由司令官定夺、实施由项目侧执行。本工作区不直接修改被审查项目代码。
