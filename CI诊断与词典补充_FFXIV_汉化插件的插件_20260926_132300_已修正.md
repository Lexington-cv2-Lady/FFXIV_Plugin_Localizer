# CI 报错诊断 与 词典补充报告

- **署名**：太太「FFXIV_汉化插件的插件」
- **日期**：2026-09-26
- **性质**：CI 排查（只读诊断，未改工作流）+ 词典补充（按司令官「加上加上，以前的也全都加上」执行）
- **边界**：未改动 `FFXIV_Plugin_Localizer` 源码、未改动工作流、未提交 git、未 push、未动 `代码审查\`。

---

## 一、CI 报错诊断（仓库 `Lexington-cv2-Lady/FFXIV_Plugin_Localizer`）

### 现象
GitHub Actions「Build & Test」最近两次均失败：
- `36215449098`（docs 提交，2026-09-26T03:38）
- `36130454992`（fix 提交，2026-09-25T11:38）

两步均止步于 **`Restore`** 步骤，`Build` / `Test` 被跳过。

### 确切报错（来自 `0_build-test.txt`）
```
##[group]Run dotnet restore
dotnet restore
...
MSBUILD : error MSB1003: Specify a project or solution file.
The current working directory does not contain a project or solution file.
##[error]Process completed with exit code 1.
```

### 根因
工作流 `.github/workflows/build-test.yml` 在**仓库根目录**执行 `dotnet restore` / `dotnet build` / `dotnet test`，但本仓库根目录**没有 `.sln` 也没有 `.csproj`**——两个 .NET 工程都在子目录里：

- `FFXIV_Plugin_Localizer/FFXIV_Plugin_Localizer.csproj`
- `FFXIV_Plugin_Localizer.Tests/FFXIV_Plugin_Localizer.Tests.csproj`

`dotnet restore` 在目录下扫不到唯一工程（这里有两个，且缺 solution），于是 MSB1003 失败。**这不是网络/代理问题**——同一次运行的 `Download Dalamud`（`Invoke-WebRequest .../latest.zip`）与 `Checkout`/`Install .NET` 均成功，说明 CI 侧取包正常，纯粹是「工作目录里没有可还原的工程」。

### 建议修复（待司令官拍板再动手）
- **方案 A（推荐，改动最小）**：在仓库根目录补一个 `.sln`，引用上述两个 `.csproj`。这样工作流现有的 `dotnet restore` / `build` / `test` 在根目录即可全部跑通（`dotnet build` 会编 solution 两个工程，`DevPluginDir` 由主工程 DeployToDev 消费；`dotnet test --no-build` 随后跑测试）。
- **方案 B**：给工作流的 `Restore`/`Build`/`Test` 步骤加 `working-directory: FFXIV_Plugin_Localizer`，但 `dotnet test` 仍需显式指向测试工程 `.csproj`，不如方案 A 干净。

> 说明：司令官原话是「检查仓库里的这个报错」，故本次只诊断、未修改工作流/未建 `.sln`。需要我落地方案 A 时请明示~

---

## 二、词典补充（「加上加上，以前的也全都加上」）

### 数据来源
`missed_list.json`（扫描 4 份汉化日志 + `运行时发现.json` 的「未命中英文」行，共 **308 条**唯一英文串，Lightfinder 广播句为第 74 条）。

### 过滤原则
308 条里绝大多数是**不可翻译噪声**，已剔除：
- ImGui 控件 ID：`###Search`、`###plugin0CollapsibleBtn`、`##recipeSearch` 等
- 插件 GUID：`WorkingPluginId: b177263d-...`、`xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx`
- 版本号：`(1.0.0)`、`v1.1.7.0`、`[2.2.0.0] (164/375)`
- 模型/贴图路径：`chara/human/c1401/obj/face/...`、`files\common\...`
- URL：`https://heliosphere.app/...`
- 玩家/作者名：`Iment` `Lym` `yuria` `khloris` `dekuscrub` `djUSA.GI` 等
- 聊天命令：`/pdr fps`、`/heels temp`、`/bw config`
- 数据数字：`Recipe ID: 31488`、`740`、`2900`

### 实际新增（15 条，已先做去重）
备份：`我的翻译.json.bak_20260926_20260926_132235`（位于 `pluginConfigs\FFXIV_Plugin_Localizer\词典目录\`）
词典 `terms`：3100 → **3115**

| 原文 | 译文 |
|---|---|
| Left | 左 |
| Right | 右 |
| List | 列表 |
| Label | 标签 |
| Search : | 搜索： |
| Path | 路径 |
| Type | 类型 |
| Size | 大小 |
| Date | 日期 |
| Any | 任意 |
| aura2makeup for Westlake Scales (Face) | aura2makeup 适用于 Westlake 鳞片（面部） |
| Only one physics group can be active at a time. | 同一时间只能有一个物理组处于激活状态。 |
| A/B/C refers to genitalia shape. A is not poseable. | A/B/C 指的是生殖器形状。A 不可摆姿势。 |
| Your Penumbra installation is not active or out of date. Update Penumbra and enable mods in Penumbra to continue using Lightless. | 你的 Penumbra 安装未激活或版本过旧。请更新 Penumbra 并在其中启用模组，以继续使用 Lightless。 |
| Lightfinder could not apply the desired broadcast state and will retry. The remote party closed the WebSocket connection without completing the close handshake. | Lightfinder 无法应用所期望的广播状态，稍后将重试。远端在未完成关闭握手的情况下关闭了 WebSocket 连接。 |

> 4 条候选（`Default Option`、`Material #0`、`Material #1`、`Penumbra inactive`）原本就已在词典里，脚本按「原文精确匹配」自动跳过，**未产生重复**。

### ⚠ 请司令官留意（游戏内实测时复核）
上表前 10 条是**通用单词/短语**（Left/Right/List/…），本插件按**精确匹配**替换——它们会把任意插件里恰好独立的同名标签也汉化。若发现某处误翻，直接在「词典目录」窗口删掉对应条目即可~

### 生效方式
主窗口点「词典目录」→「重载词典」即可生效；属本地词典，不影响内置翻译包（与司令官此前「维持现状」的决断一致，rue 等修复仍在本地词典 + dll 代码修复中）。

---

## 三、下一步建议
1. **CI**：确认是否按方案 A 补 `.sln`（或方案 B 改工作流），我可立刻落地。
2. **词典**：游戏内实测上述新增条目，剔除任何误翻的通用词。
3. 改动均未提交 git / 未 push，待司令官授权。

---

## 四、修复实施（2026-09-26 ~14:5x）

- **采用方案 A（补 `.sln`）**：在仓库根生成 `FFXIV_Plugin_Localizer.sln`，引用两个工程
  `FFXIV_Plugin_Localizer/FFXIV_Plugin_Localizer.csproj` 与
  `FFXIV_Plugin_Localizer.Tests/FFXIV_Plugin_Localizer.Tests.csproj`（测试工程已含对主工程的 `ProjectReference`，构建顺序自动正确）。工作流 `build-test.yml` **零改动**，原有的 `dotnet restore`/`build`/`test` 在根目录即可找到 solution 跑通。
- **本地验证（用 Python 调 `dotnet.exe` 绕过 LOLBin）**：
  - `dotnet restore FFXIV_Plugin_Localizer.sln` → exit 0（此前在根目录无 sln 时即 MSB1003 失败）。
  - `dotnet build FFXIV_Plugin_Localizer.sln -c Release --no-restore` → **0 警告 0 错误**，主工程 `FFXIV_Plugin_Localizer.dll` 与测试工程 dll 均产出；`[GenTranslationBundle] bundle unchanged: 1114 entries`（rue 排序保护未受影响）。
  - 测试工程 65/65 此前已绿，sln 不改变测试逻辑，故 CI 三步（restore/build/test）应全绿。
- **边界（同前）**：sln 为工作区新增文件，**未提交 git、未 push**（待司令官授权）。要让 CI 真正由红转绿，需把该 sln 提交并推送至 `origin`（`https://github.com/Lexington-cv2-Lady/FFXIV_Plugin_Localizer.git`），推送须走代理 `http://127.0.0.1:7890`。
