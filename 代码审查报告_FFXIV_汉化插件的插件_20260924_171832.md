> **【副本说明 · 防混淆】**
> - **正本路径**：`D:\Fast folder\Document\C++\代码审查\代码审查报告_FFXIV_汉化插件的插件_20260924_171832.md`
> - **性质**：本文件是审查方正式**代码审查报告**的副本（**非**项目侧自查报告，故不触发第 6 条「_已修正」处置、不属第 7 条待核对象）。
> - **出具方边界**：审查方（列克星敦「审查方」）只出报告与建议、**不改项目代码**；行动项由项目实施侧执行，涉及核心逻辑实施须司令官明示授权。
> - **权威**：以代码审查工作区正本为准。

---

# 代码审查报告 · FFXIV_汉化插件的插件

> **提出方 / 出具方**：代码审查工作区（审查方）
> **目标项目全称**：`FFXIV_汉化插件的插件`（`FFXIV_Plugin_Localizer`，C#，Dalamud 卫月插件界面运行时汉化，命令 `/plocalizer`）
> **审查时间**：2026-09-24 17:18（基于项目侧最新提交 `8d0107d` 2026-09-24 15:56）
> **署名**：列克星敦「审查方」
> **审查方式**：独立实测（编译 / `dotnet test` / 逐文件 `git diff` 核对），不采信项目侧自述；结论与项目侧最新提交时间对齐（通用母本 D.4）。

---

## 一句话结论

项目二当前状态**健康**：回归测试工程（**39 用例全绿**，`dotnet test` 审查方独立复跑通过）+ GitHub Actions CI 已补齐；最近三次提交（`401bf06` 测试工程、`81c20cd` 三处修复、`8d0107d` 自查报告）**实测无缺陷**；历史 F1–F8 除 F7 外全部闭环。尚存 **3 类轻量待办**（界面 `**` 残留、F7 配置落盘、仓库卫生），**均非回归、无核心逻辑缺陷**，无需阻塞发布准备。

---

## 行动项清单（待项目侧执行）

| # | 级别 | 事项 | 说明 |
|---|---|---|---|
| 1 | 【低】 | **剥离界面文案中的 `**` 粗体标记** | 用户可见的 `Ui.Hint` / `ImGui.SetTooltip` / `ImGui.TextColored` 中约 9 处仍含 `**…**`（如 `DictWindow.cs:131`、`MainWindow.cs:77`、`TranslationWindow.cs:72/99/114/124/125/238`、`ManualEditWindow.cs:36`）。ImGui 不渲染 markdown，游戏内会**原样显示 `**`**，违反 P0-14 / FFXIV 通用 A.3。改用「」或直接措辞。 |
| 2 | 【低】 | **F7 残留：配置落盘仍依赖失焦事件** | `AiSettingsWindow.cs:161`、`LogWindow.cs:223` 仍用 `ImGui.IsItemDeactivatedAfterEdit()` 触发 `Save()`。与 P0-23「配置改动即存，不能依赖失焦」相悖——项目自身工作记忆也点名此坑（游戏内失焦事件不可靠）。建议改为「改动即存」或失焦 + 定时兜底。 |
| 3 | 【低】 | **仓库卫生** | ①清理 `FFXIV_Plugin_Localizer.Tests/nul`（153 B，Windows 保留名，git 无法追踪；用 `dir /x` 取 8.3 短名后 `del`）；②项目二根目录堆积 **13 份**审查方/方案副本（见下「残留清单」），按第 11 条④只留最新一份、旧版备份后清除（属项目侧目录，建议项目侧自行清理或我下清理指令）。 |
| 4 | 【观察】 | **`工作记忆.json` 未提交** | `git status` 显示 modified；`git diff HEAD` 无实质内容差异（疑似行尾规范化）。按 P0-3「任务后更新工作记忆应本地提交」，建议本轮收尾时本地提交（不推送）。非缺陷。 |
| 5 | 【建议】 | **`build/add-icon-to-zip.ps1` 人工验收** | 新增打包脚本，补 DalamudPackager 15.0.0 漏打的 `images/icon.png`。headless / CI 无法验证游戏侧图标，建议在 Release 打包时人工确认图标确实进 zip。 |

> 以上均无核心逻辑改动，不涉及 B.26 红线（测试工程已覆盖高危逻辑）。**边界不变**：审查方不改项目代码；实施由项目侧执行，或司令官明示授权。

---

## 发现（详细，均实测）

### 一、回归测试工程 + CI（B.26 红线达标）✅

- **实测**：`dotnet test --configuration Release`（改道 `DevPluginDir` 避免热重载） → **失败 0 / 通过 39 / 总计 39，1 s**。审查方独立复跑，非采信自述。
- **质量**：三文件（`TextHeuristicsTests` / `ReplacementServiceTests` / `TranslationFileTests`）均为**真实 API 行为**断言，非凑数：
  - `TextHeuristics`：机翻有效性闸门（`译文==原文` 判无效、空值/纯英文/元评论/重复原文/孤立问号）、CJK 判定、`##ID` 去后缀、`IsTranslatable` 边界、**09-18 百分号误判修复回归**。
  - `ReplacementService`：**F4 闭环第四源 `_dictTerms`**（默认关 / 注入 / 清除 / 空不注入）、**四层优先级**（窗口 > 安装器 > wiki > 词典）、**跨插件排序**（M2 语义）、黑名单硬保证、`Enabled` 开关、`TryReplace` 边界（`##ID` 保留、首尾空白、前缀模板）。
  - `TranslationFile`：**原子写无 `.tmp` 残留**（通用母本 B.7）、三格式兼容、允许「译文==原文」。
- **CI**：`.github/workflows/build-test.yml`（`build + test`，失败即红）。**落地合规**。

### 二、最近三次提交代码修复（实测无缺陷）✅

`81c20cd`「fix: 体检后关窗 / 日志滚动 / ⚠符号替换」逐项核对：

1. **`Plugin.cs` `ClosePluginUiWindow`（反射关窗）**：遍历其他插件实例的 `ConfigWindow`/`MainWindow` 属性并反射调 `Close()`，全程 `try/catch` 尽力关。字符串反射对改名窗口会静默跳过，但属「尽力关」语义，**无缺陷**；代价是每个体检周期多一次反射，体检低频可接受。
2. **`LogWindow.cs` 自动滚动改 `wasAtTop` 判据**：`var wasAtTop = ImGui.GetScrollY() <= 4f;` 在绘内容前取位，仅「开自动滚动且用户本在顶部」时 `SetScrollY(0)`。逻辑正确，4f 阈值合理，**修复了「手动上滑看旧日志每帧被强制回顶」**。**无缺陷**。
3. **`⚠` → `【注意】` 符号替换**（`TranslationWindow.cs` / `WindowReplaceWindow.cs` 共 3 处）：契合 `FFXIV_游戏符号.md` 整改（⚠ 含 U+FE0F，游戏内呈 emoji 变豆腐）。**方向正确**；但**只换了 ⚠、未清同串的 `**`**（见行动项 1）——属部分修复。

`401bf06`（测试工程）、`8d0107d`（自查报告）均为新增资产 + 文档，无代码风险。

### 三、历史发现 F1–F8 闭环核对（与最新提交对齐）

| 编号 | 内容 | 状态 | 依据 |
|---|---|---|---|
| F1 | `igButton`/`igSelectable` ABI 不安全 | ✅ 闭环 | 09-16 x64 实机挂上稳定；测试第 5 类改以 `TryReplace` 覆盖替换决策（挂接须游戏内验证，已知） |
| F2 | `_entries` 非线程安全 | ✅ 闭环 | 改 `ConcurrentDictionary`（早期提交） |
| F3 | `_missed` 读端缺锁 | ✅ 闭环 | `fa7c702` `RemoveMissed` 补 `lock` |
| F4 | 自动翻译闭环缺口 | ✅ 闭环 | `fa7c702`（03:19）第四源 + 开关默认关；测试②覆盖 |
| F5 | Atk 遍历每 100ms 日志刷屏 | ✅ 处置 | `b95f4bd` 7 处空 catch 补日志 + 热路径 60 秒降频 |
| F6 | `_maxTokensLearned` 非线程安全 | ✅ 闭环 | 改 `ConcurrentDictionary` |
| **F7** | **配置仅失焦落盘** | **⚠ 仍部分存在** | `AiSettingsWindow.cs:161`、`LogWindow.cs:223` 仍用 `IsItemDeactivatedAfterEdit`（行动项 2） |
| F8 | `HealthResults` 跨线程 | ✅ 可接受 | 仅开窗口绘制，无写入竞争 |

---

## 残留清单（供第 11 条④清理参考，项目侧目录）

项目二根目录当前 13 份 `.md` 中，审查方历次送达的副本 / 方案：
`F4自动翻译闭环_参考方案_FFXIV_汉化插件的插件_v2_20260924_021252.md`、`代码审查报告_FFXIV_汉化插件的插件_第二道校验_20260924_034516.md`、`回归测试与仓库卫生_参考方案_FFXIV_汉化插件的插件_20260924_104531.md`、`回归测试工程骨架与CI模板_跨项目通用_20260924_105413.md`、`工作记忆整理与章程补立_指令_FFXIV_汉化插件的插件_20260924_130710.md`、`工作记忆整理说明_FFXIV_汉化插件的插件_20260924_133731.md`、`角色卡_FFXIV_汉化插件的插件_20260924_025815.md`、`通知_参考底稿FFXIV游戏符号库_20260924_0926.md`、`项目章程_模板与说明_跨项目通用_20260924_124834.md`、以及项目侧自查报告两份（`项目侧自查报告FFXIV_汉化插件的插件_20260924_0926.md` 属旧版，按第 12 条③应备份清除；`项目侧自查报告_FFXIV_汉化插件的插件_20260924_155432.md` 为最新）。

---

## 能力边界声明

- 本报告为**审查方正式结论**，权威归代码审查工作区；项目侧自查报告仅供过程记录。
- 审查方**不改被审查项目代码**；行动项由项目侧执行，涉及核心逻辑实施须司令官明示授权。
- 全部结论基于实测（`dotnet test` 39/39、`git diff` 逐行核对、F1–F8 与 `git log` 最新提交时间对齐），未采信项目侧「已修复」断言。
