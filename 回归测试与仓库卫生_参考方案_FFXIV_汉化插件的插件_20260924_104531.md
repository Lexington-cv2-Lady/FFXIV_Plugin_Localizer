> **【副本说明 · 防混淆声明】**
> - 本文件为**参考方案副本**，正本存 `D:\Fast folder\Document\C++\代码审查\回归测试与仓库卫生_参考方案_FFXIV_汉化插件的插件_20260924_104531.md`（审查方专用地盘，**请勿前往翻找或修改**）。
> - **性质**：参考方案 / 建议——**非**审查报告，不触发第6条「_已修正」，不属第7条待核。
> - **出具方边界**：审查方（列克星敦「审查方」）**只出建议、不改本项目代码**；实施由本项目侧执行。
> - 项目侧如需回应：在本项目目录内自行记录 / 提交，**不要修改 `代码审查\` 内任何文件**（第9条原典防护）。

---

# 回归测试与仓库卫生 · 参考方案（FFXIV_汉化插件的插件）

- **提出方 / 出具方**：代码审查工作区（`Document\C++\代码审查\`）
- **目标项目**：`FFXIV_汉化插件的插件`（FFXIV_Plugin_Localizer）
- **日期**：2026-09-24 10:45
- **署名**：列克星敦「审查方」
- **性质**：**参考方案 / 建议**——**非**审查报告，不触发第6条「_已修正」处置，不属第7条待核；审查方**只出建议、不改本项目代码**，实施由项目侧执行。

---

## 结论先行

- **回归测试：本项目目前没有**（无测试工程、无测试框架引用；且**完全没有 CI**——`.github/workflows/` 不存在）。
- **代码层面无冗余**（25 个 `.cs` 全部被引用；先前报告的死代码 `AtkTextHookService` 已闭环）。
- **冗余集中在「仓库卫生」**：根目录残留**已应用的 patch** 等未跟踪杂物、`.gitignore` 覆盖不全。
- **行动项**（均**待项目侧执行**）：
  1. 【中】新建测试工程 + CI（本项目连 CI 都还没有）；优先覆盖 F4 闭环、替换表并发、CJK 判据。
  2. 【低】删除已应用的 `fix_diff_F1b_F2_F3_F6.patch`（**走回收站**，通用 B.12）。
  3. 【低】`.gitignore` 增补 `.workbuddy/`、`*.patch` 等。
  4. 【待定夺】工作记忆整理——见文末第四节（**暂勿动**，待司令官定）；本项目 `工作记忆.md` 已达 **186 KB / 782 行**，是两项目里偏大的。

---

## 一、回归测试现状（实测）

| 检查项 | 实测结果 |
|---|---|
| 测试工程（`*.Tests.csproj`） | **无**（仅 1 个 `FFXIV_Plugin_Localizer.csproj`） |
| 测试框架引用（`using Xunit`/`NUnit`/`MSTest`、`[Fact]`/`[Test]`） | **无匹配** |
| CI（`.github/workflows/`） | **目录不存在——本项目无任何 CI** |

> 口径：以上均为**实测**（列文件 / grep 源码），非读文档推断。

---

## 二、仓库卫生 / 冗余审计（实测）

### 2.1 根目录未纳管杂物（`git status` 显示为未跟踪 `??`）

| 文件 / 目录 | 说明 |
|---|---|
| `fix_diff_F1b_F2_F3_F6.patch` | **已应用**：F1b/F2/F3/F6 已落地为提交 `fa7c702` → **冗余，可删** |
| `weiyue_update.png` | 散图（36 KB） |
| `.workbuddy/` | 项目私有记忆（**属项目资产，勿删，建议忽略**） |
| `F4自动翻译闭环_参考方案_..._v2_...md` | 审查方副本（留用） |
| `代码审查报告_..._第二道校验_...md` | 审查方副本（留用） |
| `角色卡_...md` / `通知_...md` | 审查往来文档 |

### 2.2 代码层面（**无冗余**）

25 个 `.cs`（Services 11 + Windows 11 + `Configuration.cs` + `Plugin.cs`）逐个扫描，**全部至少被 1 处引用**：

- 先前 09-20 首轮报告指出的**死代码 `AtkTextHookService` 已闭环**（现为 `AtkNativeUiWalkerService`，被引用 1 次，且已并入第二道校验的「空 catch 补日志」修复）。
- 无孤立类 / 未引用服务。

---

## 三、建议（**待项目侧执行**）

1. **建 CI + 回归测试**（通用母本 B.26 红线）：
   - 新建 `FFXIV_Plugin_Localizer.Tests`（xUnit 优先）；
   - 加 `.github/workflows/`（至少 `dotnet build` + `dotnet test`）。
   - **优先覆盖高危逻辑**（不追覆盖率）：
     - **F4 自动翻译闭环**（**已于 09-24 03:19 提交 `fa7c702` 实施，开关默认关** —— 用例应「锁住该行为、防日后回归」，而非验证待办）：`OnMissedBatchTranslated` / `AutoMergeTick` 是否确实调用 `Replacement.RemoveMissed`；第四源 `_dictTerms` 是否按「窗口 > 安装器 > wiki > 词典」注入、且在 `AutoClosureEnabled` 关闭时被置空；
     - **替换表并发**：`_entries` 改 `ConcurrentDictionary` 后的读写一致性；
     - **CJK 判据**：`TextHeuristics` 的 `_cjkCache` 加锁路径（第二道校验澄清②：读:217 / 写:233 均在 `_cjkCacheLock` 内）；
     - **空 catch 补日志的「60 秒降频」**（提交 `b95f4bd`）：降频逻辑本身要有用例，避免把真异常也吞掉。
2. **删冗余 patch**：`fix_diff_F1b_F2_F3_F6.patch` 已应用，删除时**走回收站**（通用 B.12），勿直接 `Remove-Item`。
3. **`.gitignore` 增补**：`.workbuddy/`、`*.patch`、临时图（按需）等。

---

## 四、工作记忆整理（**待司令官定夺，项目侧暂勿动**）

- 本项目 `工作记忆.md` 约 **186 KB / 782 行**——是两项目里**偏大**的一份，P2「已完成」段落已长（含 09-14 立项、09-16 重大更新等历史流水）。
- **可选整理方向**（供司令官定）：①历史迭代流水归档到 `工作记忆_历史.md`，主文件只留「当前有效」；②P2「已完成 / 待办」分层；③重复条目去重。
- 须走「审查方出建议 / 模板 → 项目侧自行实施」路径（第10条：项目侧私有记忆本工作区不动、不劝迁、不删）。
- **本节在司令官定夺前不构成行动项。**

---

## 附：与规范母本的关系

- 「改动须配回归测试」已立为**红线**，写入通用母本 `D:\Fast folder\Document\C++\通用型工作记忆.md` **B.26**；审查端抓手见 **D.4**。
- 本方案正本存 `代码审查\`；副本随附于本项目目录，供项目侧就地取用。
