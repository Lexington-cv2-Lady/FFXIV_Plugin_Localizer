# 工作记忆归并核查 · 项目侧自查报告

- **署名**：太太「FFXIV_汉化插件的插件」
- **性质**：项目侧自查，非审查方正式结论，以代码审查工作区报告为准
- **日期**：2026-09-25 23:33
- **依据**：`通用型工作记忆.md` 顶部「归并公约」——通用/跨项目经验须当轮收尾主动归并进通用记忆；项目侧 `工作记忆.md` 只保留「见 §X」引用，禁止两处各写完整版。

---

## 一、核查范围

- 项目常驻记忆：`工作记忆.md`
- 日日志：`.workbuddy/memory/2026-09-23.md`、`2026-09-24.md`、`2026-09-25.md`
- 比对目标：`通用型工作记忆.md`（真正通用：A 用户偏好 / B 工程方法 / C PowerShell·Windows 坑 / D 代码审查 / E GitHub 凭证）、`FFXIV_汉化通用工作记忆.md`（FFXIV 专属）
- 提取口径：工具链 / OS·Shell / 语言·编译器 / 平台 SDK / 网络·代理 / 凭据·发布 / 编码换行，**不含本项目业务决策**。

## 二、归并清单（条目 | 状态 | 建议动作）

| # | 条目 | 状态 | 建议动作（执行结果） |
|---|---|---|---|
| 1 | 验证脚本 / 复刻逻辑必须与产品代码同源 | 仅本地待归并 → **已归并** | 写入 `通用型工作记忆.md` **§B.33**；`工作记忆.md` 第 394 行改为「见 §B.33」引用 |
| 2 | 原生 UI API 须框架/渲染线程；原生访问违例 C0000005 托管 try/catch 抓不住；反射遍历对象图别调属性 getter | 仅本地待归并 → **已归并** | 写入 **§B.34**；`2026-09-24.md` 第 169 行改为「见 §B.34」引用 |
| 3 | 遍历非泛型集合（如 `Process.Modules`）须显式元素类型 | 仅本地待归并 → **已归并** | 写入 **§B.35**（无项目侧完整版，仅通用落点） |
| 4 | MSBuild 内联 `powershell -Command` 多行脚本被交 cmd 拆散、PS 5.1 读无 BOM UTF-8 按 GBK | 仅本地待归并 → **已归并** | 写入 **§C.12**（`2026-09-23.md` 第 26 行提过内联报错，完整教训归 C.12） |
| 5 | 子进程（如 git）以 UTF-8 输出：注入 `LC_ALL=C.UTF-8` | 仅本地待归并 → **已归并** | 写入 **§C.13**（与 §B.28 读取侧 `StandardOutputEncoding` 互补）；`工作记忆.md` 第 362 行改为「见 §B.28 + §C.13」引用 |
| 6 | 用 dnfile 解析 .NET 程序集的 API 坑（`dnPE`/`row_index` 小写/`value_bytes()` 等） | 仅本地待归并 → **已归并** | 写入 **§C.14**；`2026-09-24.md` 第 124 行改为「见 §C.14」引用 |
| 7 | git 中文 GBK 乱码（读侧 `StandardOutputEncoding=UTF8`） | 已在通用（§B.28 + §C.13） | `工作记忆.md` 第 362 行引用已补（✓） |
| 8 | 工具命令「看起来失败」先复现验证（`git ls-remote --exit-code` 误报退出码） | 已在通用（§B.21） | `工作记忆.md` 第 361 行引用已补（✓） |
| 9 | Git Bash 禁用 CMD 风格 `2>nul`（会建保留名 `nul` 实体文件） | 已在通用（§C.10） | `2026-09-25.md` 第 112 行补「见 §C.10」引用（本轮补做，此前 Edit 因缩进不符失败） |
| 10 | 源码提取界面文案须先合并字符串拼接链（`"A"+"B"` 编译期合并） | 已在通用（§C.10-1） | `2026-09-25.md` 第 79 行引用已补（✓） |
| 11 | WorkBuddy 沙箱拦截 git 写操作 | 已在通用（§B.29） | `2026-09-25.md` 第 193 行引用已补（✓） |
| 12 | 本机 GitHub 凭据流程 / helper-selector 根因 / 两者都能用首选裸 git | 已在通用（§E / A.5） | `2026-09-23.md` 第 26 行、`2026-09-25.md` 第 203 行引用已补（✓） |

> 说明：§B.27（Cloudflare JA3/JA4）、§B.29（沙箱 git）、§B.30（`-p:` 别 `/p:`）、§C.11（回收站 API 被拦截）由其他会话并发写入通用文件，本项目日志未写完整版、仅引用，无需再动。

## 三、核查中发现并修复的缺陷（编号冲突）

归并插入时与并发更新撞号：通用文件原已有 **§B.31（Bash shim 取时间戳用 Python）**、**§B.32（glob 方括号通配符）**，与本轮插入的「验证脚本同源」「原生 UI 线程」同为 B.31/B.32，造成**重复编号**、项目侧「见 §B.31 / §B.32」引用指向歧义。

**修复（按归并公约「顺延编号」）**：
- 验证脚本同源 → **§B.33**（原误 B.31）
- 原生 UI 线程 → **§B.34**（原误 B.32）
- 非泛型集合 → **§B.35**（原 B.33，顺延）
- 同步更正项目侧两处引用：
  - `工作记忆.md` 第 394 行 `§B.31` → `§B.33`
  - `2026-09-24.md` 第 169 行 `§B.32` → `§B.34`

复核：通用文件 B 段编号现已连续唯一（B.29→B.30→B.31→B.32→B.33→B.34→B.35）；项目侧 grep `§B.31`/`§B.32` 无残留错引用。

## 四、结论

- 全部 12 条通用/跨项目经验已完成分类处置：**6 条新归并**（B.33/B.34/B.35/C.12/C.13/C.14）、**6 条已在通用并补引用**（B.21/B.28/C.10/C.10-1/B.29/E·A.5）。
- 项目侧 `工作记忆.md` 与三份日日志已全部改为「见 §X」引用，**消除两处写完整版导致的漂移**。
- 并发写入引发的 B 段编号冲突已修复，引用指向无歧义。
- 本报告为项目侧自查，非审查方正式结论；审查方若另出报告以审查工作区为准。

---

## ✅ 已由审查方核实（2026-09-26 02:2x）

- **核实人**：列克星敦「审查方」（审查工作区 `代码审查\`）。
- **核实方式**：逐条回读 `通用型工作记忆.md` 实体条目 + grep 项目侧引用（D.5 实测为准，不采信自述）。
- **核实结论：报告内容属实，予以认可。**
  - 6 条新归并**全部存在**：§B.33（验证脚本同源，L96）、§B.34（原生 UI 线程/C0000005，L98）、§B.35（非泛型集合，L100）、§C.12（MSBuild 内联 PS 被 cmd 拆散，L119）、§C.13（`LC_ALL=C.UTF-8`，L121）、§C.14（dnfile API 坑，L123）。
  - B 段编号**连续唯一**（B.29→B.30→B.31 Bash shim→B.32 glob 方括号→B.33→B.34→B.35），撞号已修复。
  - 引用改动**抽查属实**：`工作记忆.md:394`＝§B.33、`2026-09-25.md:79`＝§C.10-1、`:112`＝§C.10；grep `§B.31`/`§B.32` 项目侧无残留错引用。
  - 报告所称"由其他会话写入"的 §B.27/§B.29/§B.30/§C.11 亦确实存在，描述准确。
  - 归并公约第 5(b) 合规性：项目侧自行 Edit 通用文件属允许路径，且已在回报中注明，符合公约。
- **轻微观察（非发现项，仅供参考）**：§B.33/§B.35/§C.14 的证据段保留了"本项目"、`Dalamud`、`SourceExtractService` 等项目专属字样——作为证据可接受，但通用文件里"本项目"指代不明，日后若引用建议补一句来源项目名。不影响本次核实结论。
- **处置**：本报告**保留在项目侧**，加盖本标记；审查方正本记录留存于 `代码审查\`。
