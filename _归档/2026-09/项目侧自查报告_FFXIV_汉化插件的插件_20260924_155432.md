# 项目侧自查报告 —— FFXIV_汉化插件的插件（回归测试工程 + CI）

- **署名**：太太「FFXIV_汉化插件的插件」
- **日期**：2026-09-24 15:54
- **角色**：实施侧 AI（非审查方）
- **依据**：审查方副本 `回归测试工程骨架与CI模板_跨项目通用_20260924_105413.md` + 司令官 2026-09-24 授权指令
- **声明**：**本项目侧自查，非审查方正式结论，以代码审查工作区报告为准。**

---

## 一、任务来源与授权边界

司令官授权实施侧落地「测试工程 + CI」，明确要求：用真实 API 覆盖项目高危逻辑（勿 mock 核心判定）、与现有代码改动**分离提交**、**只本地提交绝不推送**、拿不准或涉及较大核心改动先停下留报告。本 AI 严格在授权边界内作业，**未擅动汉化核心**（`RebuildMerged` 与活动替换表 `_table`）、**未代点 UI / 未翻 `代码审查\` / 未做云账号 / 未做运行时拼接采集**。

## 二、已落实项

| # | 项 | 状态 | 落点 |
|---|---|---|---|
| 1 | 新建测试工程 `FFXIV_Plugin_Localizer.Tests`（xUnit，net10.0-windows7.0，引用主工程） | ✅ | `FFXIV_Plugin_Localizer.Tests/` |
| 2 | 覆盖 5 类高危逻辑真实行为（39 条用例） | ✅ | 同上 |
| 3 | CI：`.github/workflows/build-test.yml`（Release 构建+测试，失败即红） | ✅ | `.github/workflows/build-test.yml` |
| 4 | `.gitignore` 并入本次提交 | ✅ | `.gitignore` |
| 5 | `dotnet test` 全绿 | ✅ | 39 通过 / 0 失败 |
| 6 | 测试工程+CI 与现有代码改动**分离提交**（本地、未推送） | ✅ | commit `401bf06` / `81c20cd` |
| 7 | `工作记忆.md` 同步更新（最后更新 / 当前状态 / P2 待办） | ✅ | `工作记忆.md` |
| 8 | 本项目侧自查报告 | ✅ | 本文件 |

## 三、5 类高危逻辑覆盖与真实行为校准

> 全部用**真实 API**断言，核心判定零 mock。过程中发现并修正了与审查模板示例**相反**的两条假设，均按真实代码校准。

1. **`TextHeuristics` 文案判定**（`TextHeuristicsTests.cs`）
   - `IsValidMachineTranslation(en,zh)`：对「译文==原文」返回 **false**（机翻闸门——同文=没翻应拒收；**与审查模板示例「同文=有效」相反**，已按真实行为修正断言）。
   - `HasCjk`（中/片假名 true、纯英 false）、`StripIdSuffix`、`IsTranslatable` 边界（`Ctrl`/`F5`/`#FFFFFF`/`/tp` false，`Attack` true）、`% damage` 可翻（`%` 误判修复）、`Remaining: %d` 不可翻。
2. **`ReplacementService` 第四源 `_dictTerms`（F4 闭环）**（`ReplacementServiceTests.cs`）
   - `AutoClosureEnabled` 默认 **false**（未开启时行为同前）；`SetDictTerms` 注入 / 清除 / 空三态；四层优先级（窗口>安装器>wiki>dict，`AddMerged` 已有键跳过）。
3. **四层替换源优先级**：跨插件排序（AAA/BBB 文件、`XX` 双字符键）、黑名单、`Enabled`、`##ID` 保留（含 `##café_id` 字节/字符下标）、首尾空白、前缀模板、`Count`/未命中。
   - **校准**：`Normalize` 要求键长 **≥ 2**（单字符键被丢弃）→ 测试键全改为 `AA/BB/Key/XX` 等双字符；`TryReplace` 热路径 `if (!Enabled || _table.Count == 0 || n < 2 || n > 1024) return 0`。
4. **引用格式 `{{...}}` 解析** —— **⚠ 代码无此实现**（grep 全工程 + 审查/回归参考方案均无），按指令「拿不准先停下 / 按真实代码覆盖」**转述为缺失项**，未编造测试。（见第四节遗留①）
5. **Atk 控件标签遍历** —— `AtkNativeUiWalkerService` 依赖 `AtkStage.Instance()` 等游戏运行时，**无法 headless**；按指令「需运行时先停下」，改为用 `TryReplace(byte*,int)` 覆盖**替换决策核心**（##ID 保留 / 首尾空白 / 前缀模板），igButton/igSelectable 标签替换的挂接**须游戏内验证**。（见第四节遗留②）

## 四、遗留与待办（须向司令官/审查方披露）

1. **第 4 类「引用格式 `{{...}}` 解析」代码未实现**：当前替换层不含 `{{...}}` 占位引用解析。若司令官确认需要该能力，应单独立项实现后再补测试；本次不擅加。
2. **第 5 类 Atk 控件标签遍历须游戏运行时验证**：`ReplacementService.TryReplace` 的替换决策已 headless 覆盖，但 Atk 原生 UI 的实际遍历与 igButton/igSelectable 标签挂接的**实机生效**仍需进游戏验证（受 UIPI/运行时限制，headless 不可测）。
3. **`nul` 保留名残留（本地、非紧急）**：测试工程中曾误建 Windows 保留名文件 `FFXIV_Plugin_Localizer.Tests/nul`（153 字节错误输出残留）。git 无法索引、常规删除被 Windows 保留名机制拦截（ACCESS_DENIED / 找不到），**未进任何提交**。待以专用手段清理：在**管理员 Command Prompt** 用 `dir /x` 取该目录的 8.3 短名后 `del <短名>`，或用 `\\?\` 扩展路径 + 原生 API 删除。属本地非紧急残留，不影响仓库与交付。
4. **CI 依赖 Dalamud 运行时**：`build-test.yml` 已用「Download Dalamud」步骤解压到 `%APPDATA%\XIVLauncher\addon\Hooks\dev` 提供最小托管依赖；本地测试用 `--property:DevPluginDir=/tmp/fpl_devplugins` 改道 `DeployToDev`，避免热重载打断司令官可能的长任务。CI 实跑结果以 PR/推送后 GitHub Actions 为准（本次未推送，未触发线上 CI）。

## 五、提交与推送状态

- `401bf06` test: 新增回归测试工程与 CI（6 files, +486）
- `81c20cd` fix: 体检后关闭插件窗口 / 日志自动滚动优化 / 游戏内不可显示符号替换（8 files, +71/-244，含删两份旧整改报告、新增 build 脚本）
- **两者均为本地提交，未推送（遵循 P0-1 推送只听司令官明说）。**
- 校验：`dotnet test` 39 条全绿（HEAD 重跑确认）。

## 六、结论

测试工程与 CI 已按授权落地并**全绿**；护栏全部遵守（分离提交 / 不推送 / 不动核心 / 不越界）；第 4 类缺失、第 5 类需运行时验证、以及 `nul` 本地残留已如实披露。**本结论为项目侧自查，非审查方正式结论；审查方（另一「太太」）将独立校验兜底。**

## 七、附：关窗功能修复补记（项目侧，2026-09-24 晚）

- **问题**：本报告第五节所列 `81c20cd` 含「体检后关闭插件窗口」，但司令官复核发现**该功能实际上没生效**。日志实证：一次 28 个插件的体检，仅 1 个 Atk 原生窗（LazyGathererConfig）被关，其余 27 个 ImGui 窗纹丝不动。
- **根因**：原 `ClosePluginUiWindow` 直接对 `IExposedPlugin` **包装对象**反射 `ConfigWindow`/`MainWindow`；但官方 `IExposedPlugin` 只暴露 `Name/InternalName/IsLoaded/HasConfigUi/OpenConfigUi/OpenMainUi`，**不暴露插件实例**（用 `System.Reflection.Metadata` 探针扫 Dalamud.dll 实锤）→ 反射拿 null → 静默 no-op。
- **修复（方案A，司令官授权直接实施）**：反射**具体运行时类型**取出真正的 `IDalamudPlugin` 实例，再遍历其所有带 `IsOpen` 的 Window 成员置 `IsOpen=false`（Dalamud `Window` 无 `Close()`，标准关窗做法即 `IsOpen=false`）。Atk 原生窗仍由 `TryClosePluginWindow` 负责，两套互补。
- **落点**：`Plugin.cs` 新增 `ClosePluginUiWindowCore`（静态可测核心）+ 6 个辅助反射方法；csproj 加 `InternalsVisibleTo` 授权测试工程；新增 `ClosePluginUiWindowTests` 沙盒测试 3 条（无头，用类名命中 `IsPluginInstance` 兜底分支，不依赖 Dalamud 运行时）。
- **验证**：`dotnet test` 总计 **42 全绿**（原 39 + 新 3）；`dotnet build` 零警告零错误并已构建即部署到 devPlugins（dll 时间戳 18:03）。**ImGui 窗关闭的实机生效仍须进游戏跑一次体检确认**（headless 仅验证反射机制）。
- **提交**：`7b3a43c`（本地，未推送，遵循 P0-1）。
